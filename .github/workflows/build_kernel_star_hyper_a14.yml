name: Build Kernel Star Hyper A14

on:
  workflow_dispatch:
    inputs:
      PROFILE_NAME:
        description: "Nome do arquivo de perfil em Profiles/ (ex.: star_hyper_a14.env)"
        required: true
        default: "star_hyper_a14.env"

jobs:
  build:
    runs-on: ubuntu-22.04

    env:
      # deixa explícito que o alvo é arm64 em todas as chamadas de make
      ARCH: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Carregar perfil
        id: load_env
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p Profiles
          PROFILE_PATH="Profiles/${{ github.event.inputs.PROFILE_NAME }}"
          if [[ ! -f "$PROFILE_PATH" ]]; then
            echo "::error::Arquivo de perfil não encontrado: $PROFILE_PATH"
            ls -la Profiles || true
            exit 2
          fi
          echo "→ Usando perfil: $PROFILE_PATH"
          sed -i 's/\r$//' "$PROFILE_PATH"
          while IFS= read -r raw || [[ -n "$raw" ]]; do
            [[ -z "$raw" || "$raw" =~ ^[[:space:]]*# ]] && continue
            if printf '%s' "$raw" | grep -Eq '^[A-Za-z_][A-Za-z0-9_]*=.+$'; then
              k="${raw%%=*}"; v="${raw#*=}"
              v="${v%\"}"; v="${v#\"}"; v="${v%\'}"; v="${v#\'}"
              export "$k=$v"
              echo "$k=$v" >> "$GITHUB_ENV"
              printf '  %s=%s\n' "$k" "$v"
            fi
          done < "$PROFILE_PATH"
          [[ -n "${KERNEL_SOURCE:-}" ]]  || { echo "::error::KERNEL_SOURCE não definido no perfil"; exit 2; }
          [[ -n "${DEFCONFIG_NAME:-}" ]] || { echo "::error::DEFCONFIG_NAME não definido no perfil"; exit 2; }

      - name: (Opcional) Baixar LLVM/Clang do perfil
        if: ${{ env.CLANG_SOURCE != '' }}
        shell: bash
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y xz-utils curl
          mkdir -p toolchains && cd toolchains
          curl -L "$CLANG_SOURCE" -o clang.tar.xz
          tar -xJf clang.tar.xz
          CLANG_DIR="$(find "$(pwd)" -maxdepth 1 -type d -name '*clang*' | head -n1)"
          echo "CLANG_DIR=$CLANG_DIR" >> "$GITHUB_ENV"

      - name: Instalar toolchain fallback (se não baixou clang)
        if: ${{ env.CLANG_SOURCE == '' }}
        shell: bash
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y build-essential bc bison flex libssl-dev \
                                  libncurses5-dev git wget curl jq python3 ccache \
                                  clang lld llvm

      - name: Clonar fonte do Kernel (robusto a branch ausente)
        id: clone_kernel
        shell: bash
        run: |
          set -euxo pipefail
          rm -rf kernel-src || true
          if [[ -n "${KERNEL_REF:-}" ]]; then
            git clone --depth 1 --branch "$KERNEL_REF" "$KERNEL_SOURCE" kernel-src
          else
            if [[ -n "${KERNEL_BRANCH:-}" ]] && git ls-remote --exit-code --heads "$KERNEL_SOURCE" "$KERNEL_BRANCH" >/dev/null 2>&1; then
              git clone --depth 1 --branch "$KERNEL_BRANCH" "$KERNEL_SOURCE" kernel-src
            else
              default_branch="$(git ls-remote --symref "$KERNEL_SOURCE" HEAD | awk '/^ref:/ {print $2}' | sed 's#refs/heads/##')"
              git clone --depth 1 --branch "$default_branch" "$KERNEL_SOURCE" kernel-src
            fi
          fi
          echo "KERNEL_DIR=$GITHUB_WORKSPACE/kernel-src" >> "$GITHUB_ENV"

      - name: Injetar KernelSU (normaliza layout em drivers/kernelsu + symlink CamelCase)
        shell: bash
        run: |
          set -euxo pipefail
          cd "$KERNEL_DIR"
          rm -rf KernelSU drivers/kernelsu drivers/KernelSU ksu_tmp || true
          mkdir -p drivers
          git clone --depth 1 --branch "${KERNELSU_BRANCH:-main}" "${KERNELSU_SOURCE}" ksu_tmp
          CAND_DIR=""
          for d in "ksu_tmp/KernelSU" "ksu_tmp/kernel" "ksu_tmp"; do
            if [[ -f "$d/Makefile" ]]; then CAND_DIR="$d"; break; fi
          done
          [[ -n "$CAND_DIR" ]] || { echo "::error::Não achei Makefile do KernelSU no repositório"; exit 2; }
          mkdir -p drivers/kernelsu
          cp -a "${CAND_DIR}/." drivers/kernelsu/
          if [[ ! -f drivers/kernelsu/Kconfig && -f drivers/kernelsu/kernel/Kconfig ]]; then
            cp -f drivers/kernelsu/kernel/Kconfig drivers/kernelsu/Kconfig
          fi
          ln -sfn kernelsu drivers/KernelSU
          sed -i '/obj-y[[:space:]]\+\+=\s*KernelSU\//d' drivers/Makefile || true
          sed -i '/source\s*"drivers\/KernelSU\/Kconfig"/d' drivers/Kconfig || true
          grep -q 'KernelSU/include' drivers/Makefile 2>/dev/null || \
            echo 'subdir-ccflags-y += -I$(srctree)/KernelSU/include' >> drivers/Makefile
          grep -qE '(^|[[:space:]])obj-y[[:space:]]*\+=[[:space:]]*kernelsu/' drivers/Makefile 2>/dev/null || \
            echo 'obj-y += kernelsu/' >> drivers/Makefile
          if ! grep -q 'drivers/kernelsu/Kconfig' drivers/Kconfig 2>/dev/null; then
            printf '\nsource "drivers/kernelsu/Kconfig"\n' >> drivers/Kconfig
          fi
          awk '!x[$0]++' drivers/Makefile > .mk && mv .mk drivers/Makefile
          awk '!x[$0]++' drivers/Kconfig  > .kc && mv .kc drivers/Kconfig

      - name: Sanear Makefiles/Config (CRLF/BOM/TAB)
        shell: bash
        run: |
          set -euxo pipefail
          cd "$KERNEL_DIR"
          git ls-files -z \
            | xargs -0 file --mime-type \
            | grep 'text/' \
            | cut -d: -f1 \
            | xargs -r sed -i 's/\r$//'

      - name: Hotfix EROFS (unused variable com -Werror)
        shell: bash
        run: |
          set -euxo pipefail
          cd "$KERNEL_DIR"
          f="fs/erofs/inode.c"
          if [[ -f "$f" ]] && grep -qE '\bloff_t[[:space:]]+off;' "$f"; then
            sed -i 's/\bloff_t[[:space:]]\+off;/__maybe_unused loff_t off;/' "$f"
            echo "→ Hotfix aplicado em $f"
          fi

      - name: Limpeza de vestígios de build no source
        shell: bash
        run: |
          set -euxo pipefail
          cd "$KERNEL_DIR"
          rm -rf include/config include/generated arch/arm64/include/generated .config .config.old 2>/dev/null || true
          mkdir -p out

      - name: Configurar e compilar (robusto)
        shell: bash
        env:
          LLVM: 1
          CC: clang
          LD: ld.lld
          HOSTCC: clang
          HOSTCXX: clang++
        run: |
          set -euo pipefail
          cd "$KERNEL_DIR"

          # =========================
          # Resolver DEFCONFIG (robusto)
          # =========================
          want="${DEFCONFIG_NAME#./}"          # remove ./ se vier
          want="${want#arch/arm64/configs/}"   # normaliza se vier com prefixo
          base="${want##*/}"
          base_no_vendor="${base#vendor/}"     # ex.: vendor/foo_defconfig -> foo_defconfig

          found=""

          # 1) Caminho exato informado
          if [[ -f "arch/arm64/configs/$want" ]]; then
            found="$want"
          fi

          # 2) Se não achou, tente variações do mesmo nome
          if [[ -z "$found" ]]; then
            for try in \
              "$base" \
              "vendor/$base" \
              "$base_no_vendor" \
              "vendor/$base_no_vendor"
            do
              if [[ -f "arch/arm64/configs/$try" ]]; then
                found="$try"
                break
              fi
            done
          fi

          # 3) Se ainda não achou, procure por arquivo com o mesmo *basename* no repositório
          if [[ -z "$found" ]]; then
            cand="$(git ls-files arch/arm64/configs | grep -E "/(${base}|${base_no_vendor})$" | head -n1 || true)"
            if [[ -n "$cand" ]]; then
              found="${cand#arch/arm64/configs/}"
            fi
          fi

          # 4) Último recurso: fallbacks comuns de GKI
          if [[ -z "$found" ]]; then
            for fb in "vendor/gki_defconfig" "gki_defconfig"; do
              if [[ -f "arch/arm64/configs/$fb" ]]; then
                echo "::warning::Defconfig '${want}' não encontrado. Usando fallback: $fb"
                found="$fb"
                break
              fi
            done
          fi

          # 5) Se mesmo assim não achou, loga candidatos e falha explicitamente
          if [[ -z "$found" ]]; then
            echo "::error::Nenhum defconfig compatível encontrado para '${want}'."
            echo "Alguns defconfigs disponíveis:"
            find arch/arm64/configs -maxdepth 2 -name "*defconfig" | head -n 200 || true
            exit 2
          fi

          echo "→ Defconfig selecionado: $found"

          # =========================
          # Geração de .config (não-interativa)
          # =========================
          # Limpeza esquemática já foi feita antes; garantimos OUT
          mkdir -p out

          # 1) defconfig base
          make O=out ARCH=arm64 "$found" V=1      2>&1 | tee build.log

          # 2) sincroniza sem interação
          make O=out ARCH=arm64 olddefconfig      2>&1 | tee -a build.log

          # 3) Desliga Werror e zram/zsmalloc para evitar abortos por warnings
          chmod +x ./scripts/config
          ./scripts/config --file out/.config -d CONFIG_ZRAM        || true
          ./scripts/config --file out/.config -d CONFIG_ZSMALLOC    || true
          ./scripts/config --file out/.config -d CONFIG_WERROR      || true
          ./scripts/config --file out/.config -d CONFIG_CC_WERROR   || true
          make O=out ARCH=arm64 olddefconfig      2>&1 | tee -a build.log

          # (opcional) mostra estados
          grep -E 'CONFIG_(ZRAM|ZSMALLOC|WERROR|CC_WERROR)=' out/.config || true

          # =========================
          # Build
          # =========================
          # Warnings não viram erro; força LLVM/Clang; host em clang também
          KBUILD_CFLAGS="-Wno-error" KCFLAGS="-Wno-error" WERROR=0 \
          make -j"$(nproc)" \
               O=out ARCH=arm64 LLVM=1 CC=clang LD=ld.lld \
               HOSTCC=clang HOSTCXX=clang++ V=1 \
               2>&1 | tee -a build.log


      - name: Diagnóstico de falha (se houver)
        if: failure()
        shell: bash
        run: |
          set -euxo pipefail
          cd "$KERNEL_DIR"
          echo "=== HEAD do log (200 linhas) ==="; head -n 200 build.log || true
          echo "=== TAIL do log (200 linhas) ==="; tail -n 200 build.log || true
          echo "=== Erros encontrados (±3) ==="
          grep -nEi -C3 '(^|\s)(error:|fatal error:|undefined reference|No rule to make target|No such file or directory)' build.log || true
          echo "=== .config (parcial) ==="
          test -f out/.config && sed -n '1,140p' out/.config || true

      - name: Upload de logs e .config
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-kernel-logs
          path: |
            ${{ env.KERNEL_DIR }}/build.log
            ${{ env.KERNEL_DIR }}/out/.config

      - name: Empacotar com AnyKernel3
        if: success()
        shell: bash
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE"
          rm -rf AnyKernel3
          git clone --depth 1 --branch "${AK3_BRANCH:-master}" "${AK3_SOURCE}" AnyKernel3
          KIMG="$KERNEL_DIR/out/arch/arm64/boot/Image.gz-dtb"
          if [[ ! -f "$KIMG" ]]; then
            echo "::error::Kernel Image não encontrado em $KIMG"
            ls -la "$KERNEL_DIR/out/arch/arm64/boot" || true
            exit 2
          fi
          cp -fv "$KIMG" AnyKernel3/
          cd AnyKernel3
          zip -r9 "../kernel-${ROM_TEXT:-build}.zip" . -x "*.git*"
          echo "ZIP_PATH=$GITHUB_WORKSPACE/kernel-${ROM_TEXT:-build}.zip" >> "$GITHUB_ENV"

      - name: Publicar artefato
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ env.ROM_TEXT || 'build' }}
          path: ${{ env.ZIP_PATH }}
